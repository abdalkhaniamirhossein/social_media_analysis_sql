# 📊 Social Media Data Analytics (MySQL)

## 🧩 Project Overview
This project performs **exploratory data analysis (EDA)** on a social media dataset using **pure SQL** in **MySQL**.  
The goal is to uncover insights about **platform popularity**, **language usage**, **user sentiment**, **hashtags**, and **weekly posting behavior**.

---

## 🎯 Key Objectives
- Identify which **social media platforms** have the most posts  
- Compare **languages** used across posts  
- Analyze **sentiment distribution** (positive, negative)  
- Measure **average post text length** by platform  
- Track **top hashtags** and their sentiment scores  
- Discover **activity patterns** by day of the week  

---

## 🛠️ Tools & Technologies
- **SQL (MySQL)**  
- **MySQL Workbench**  
- Dataset: `socialmedia` table (columns include platform, language, sentiment_label, text_content, hashtags, shares_count, day_of_week)

---

## 🧮 Key SQL Techniques Used
- **Aggregation**: `COUNT`, `AVG`, `ROUND`, `SUM`  
- **Conditional logic**: `CASE WHEN`  
- **Grouping & sorting**: `GROUP BY`, `ORDER BY`, `HAVING`  
- **Filtering**: `WHERE`, `IN`, and logical operators  
- **Ranking & comparison** using `LIMIT`, `DESC`, and `FIELD()`  

---
